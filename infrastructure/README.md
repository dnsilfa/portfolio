# Infrastructure

The `website/` directory is the Terraform root module for the portfolio hosting infrastructure. Terraform currently runs locally and uses HCP Terraform for state storage and history.

No AWS resources are defined yet. See `website/README.md` for setup instructions.
